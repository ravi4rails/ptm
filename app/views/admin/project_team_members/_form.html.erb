<%= form_for [:admin, @project_team_member] do |f| %>
  <% if @project_team_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_team_member.errors.count, "error") %> prohibited this project_team_member from being saved:</h2>

      <ul>
      <% @project_team_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :role %>
    <%= f.text_field :role, id: :project_team_member_role, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :Project %>
    <br>
    <%= f.select :project_id, options_for_select(project_list, selected: @project.name), { include_blank: "Select Project" }, class: "form-control"%>
  </div>

  <div class="form-group">
    <%= f.label :User %>
    <br>
    <%= f.select :user_id, options_for_select(user_list, selected: @user.name), { include_blank: "Select User" }, class: "form-control"%>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
<%= tinymce %>